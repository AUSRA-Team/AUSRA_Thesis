% ============================================================================
% AUSRA Thesis - Preamble (Hybrid MIT-ASU Professional Style)
% ============================================================================

% --- Essential Packages ---
\usepackage[utf8]{inputenc}
\usepackage[T1]{fontenc}
\usepackage[english]{babel}
\usepackage{csquotes} % Recommended for biblatex

% --- Professional Typography via MIT Style ---
\usepackage{microtype}              % Character protrusion and font expansion
\usepackage{newtxtext}              % Times-like text font (High quality)
\usepackage{newtxmath}              % Matching math font

% --- Page Layout (Strict ASU Guidelines) ---
% Left/Inner: 3cm, Right/Outer: 2.5cm, Top: 2.5cm, Bottom: 2cm
\usepackage[
    a4paper,
    top=2.5cm, 
    bottom=2cm, 
    inner=3cm, 
    outer=2.5cm, 
    headheight=14pt,
    bindingoffset=0cm % Managed by inner/outer margins for twoside
]{geometry}

% --- Paragraph Settings (ASU) ---
% No indentation for first line, 12pt space before
\setlength{\parindent}{0pt}
\setlength{\parskip}{12pt}

% --- Line Spacing (ASU: 1.5 lines) ---
\usepackage{setspace}
\onehalfspacing 

% --- Headers and Footers (ASU) ---
\usepackage{fancyhdr}
\pagestyle{fancy}
\fancyhf{}
% Header: Chapter title on the right (10pt)
\renewcommand{\chaptermark}[1]{\markboth{#1}{}}
\fancyhead[R]{\fontsize{10}{12}\selectfont\leftmark}
\fancyhead[L]{}
% Footer: Page number in the middle (10pt)
\fancyfoot[C]{\fontsize{10}{12}\selectfont\thepage}
\renewcommand{\headrulewidth}{0.4pt}
\renewcommand{\footrulewidth}{0pt}

% Plain style for chapter opening pages
\fancypagestyle{plain}{
    \fancyhf{}
    \fancyfoot[C]{\fontsize{10}{12}\selectfont\thepage}
    \renewcommand{\headrulewidth}{0pt}
}

% --- Mathematics & Science ---
\usepackage{amsmath, amsthm, amsfonts}
\usepackage{mathtools}
\usepackage[version=4]{mhchem}    % Chemical formulas (from MIT template)
\usepackage{siunitx}              % Units
\sisetup{per-mode=symbol, bracket-unit-denominator=false}

% --- Graphics and Figures ---
\usepackage{graphicx}
\usepackage{float}
\usepackage{subfig}
\graphicspath{{figures/}}

% Define \degree command since it is used but not defined
\providecommand{\degree}{\ensuremath{^{\circ}}}

% --- Tables ---
\usepackage{booktabs}             % Professional tables (MIT style)
\usepackage{array}
\usepackage{multirow}
\usepackage{longtable}
\usepackage{tabularx}
\usepackage{dcolumn}              % Decimal alignment (MIT style)

% --- Captions (ASU + Professional) ---
\usepackage[labelfont=bf,textfont=bf,singlelinecheck=false,justification=centering,skip=6pt]{caption}
\DeclareCaptionFont{tenpt}{\fontsize{10}{12}\selectfont}
\captionsetup{font=tenpt}
\captionsetup[table]{position=top, skip=6pt}
\captionsetup[figure]{position=bottom, skip=6pt}

% --- Numbering ---
\numberwithin{equation}{chapter}
\numberwithin{figure}{chapter}
\numberwithin{table}{chapter}

% --- Headings (ASU + Professional Expansion) ---
\usepackage{titlesec}

% Chapter (Heading 1): 16pt, Bold, Small Caps, Expanded Spacing, Centered
\titleformat{\chapter}[display]
  {\normalfont\bfseries\scshape\centering\fontsize{16}{19}\selectfont}
  {\chaptertitlename\ \thechapter}
  {12pt}
  {\textls[200]{\fontsize{16}{19}\selectfont}} % Expanded by ~200 tracking

\titlespacing*{\chapter}{0pt}{-20pt}{12pt}

% Section (Heading 2): 14pt, Bold, Title Case
\titleformat{\section}
  {\normalfont\bfseries\fontsize{14}{17}\selectfont}
  {\thesection}
  {1em}
  {}
\titlespacing*{\section}{0pt}{6pt}{6pt}

% Subsection (Heading 3): 14pt, Bold, Title Case
\titleformat{\subsection}
  {\normalfont\bfseries\fontsize{14}{17}\selectfont}
  {\thesubsection}
  {1em}
  {}
\titlespacing*{\subsection}{0pt}{6pt}{6pt}

% --- Colors ---
\usepackage[table,xcdraw]{xcolor}
\definecolor{asuBlue}{RGB}{0, 61, 121}
\definecolor{asuGold}{RGB}{255, 196, 37}
\definecolor{backcolour}{rgb}{0.95,0.95,0.92}
\definecolor{codegray}{rgb}{0.5,0.5,0.5}

% --- Hyperlinks (Professional Blue) ---
\usepackage[
    colorlinks=true,
    linkcolor=asuBlue,
    citecolor=asuBlue,
    urlcolor=asuBlue,
    bookmarks=true,
    bookmarksnumbered=true,
    pdfauthor={AUSRA Team},
    pdftitle={Autonomous Unified Swarm Robotics Architecture},
    pdfsubject={Graduation Project 2026}
]{hyperref}

% --- Bibliography (MIT Style: Numeric-Comp, No "In:") ---
\usepackage[
    backend=biber,
    style=numeric-comp,    % [1-3] instead of [1],[2],[3]
    sorting=none,          % Citations in order of appearance
    maxbibnames=10,        % MIT Template limit
    giveninits=true        % G. O. Young instead of George O. Young
]{biblatex}
\addbibresource{backmatter/references.bib}

% Remove "In:" for articles
\renewbibmacro{in:}{%
  \ifentrytype{article}{}{\printtext{\bibstring{in}\intitlepunct}}}

% --- Table of Contents (Formal Style) ---
\makeatletter
\let\c@lofdepth\relax
\let\c@lotdepth\relax
\makeatother
\usepackage{tocloft}

\renewcommand{\cfttoctitlefont}{\hfill\Large\bfseries\MakeUppercase}
\renewcommand{\cftaftertoctitle}{\hfill}
\renewcommand{\cftloftitlefont}{\hfill\Large\bfseries\MakeUppercase}
\renewcommand{\cftafterloftitle}{\hfill}
\renewcommand{\cftlottitlefont}{\hfill\Large\bfseries\MakeUppercase}
\renewcommand{\cftafterlottitle}{\hfill}

\renewcommand{\cftchapfont}{\bfseries}
\renewcommand{\cftchappagefont}{\bfseries}
\renewcommand{\cftchapleader}{\cftdotfill{\cftdotsep}}
\setlength{\cftbeforechapskip}{1.0em}
\setlength{\cftbeforesecskip}{0.3em}
\setlength{\cftchapindent}{0pt}
\setlength{\cftsecindent}{1.5em}

% --- Code Listings ---
\usepackage{listings}
\lstset{
    backgroundcolor=\color{backcolour},   
    commentstyle=\color{codegray},
    keywordstyle=\color{magenta},
    numberstyle=\tiny\color{codegray},
    basicstyle=\ttfamily\footnotesize,
    breaklines=true,                 
    keepspaces=true,                 
    numbers=left,                    
    numbersep=5pt,                  
    frame=lines
}

% --- Algorithms ---
\usepackage[ruled,vlined,linesnumbered]{algorithm2e}

% --- Diagrams ---
\usepackage{tikz}
\usetikzlibrary{shapes, arrows, positioning, calc, fit}

% --- Custom Boxes ---
\usepackage[most]{tcolorbox}
\newtcolorbox{notebox}{colback=blue!5, colframe=asuBlue, title={\textbf{Note}}, fonttitle=\bfseries, boxrule=1pt, arc=3pt}

% --- Acronyms ---
\usepackage[acronym, toc, nopostdot]{glossaries}
\makeglossaries

% --- Custom Commands ---
\newcommand{\ausra}{\textsc{AUSRA}}
% --- Appendix ---
\usepackage[toc,page]{appendix}
